
// sum all the numbers between 0 - 1000 without using a loop 
// or an equation;  

 
function sum(s, v, e){
	 var array = [];
	 var (array.push(x))*e; 
   var x = (s+v) 
   return array; 
}
console.log(sum(0, 1, 1000));

//recursion = when a function calls itself; 
// you can stop it from calling itself with an 
// if statement; 


// fact

cons